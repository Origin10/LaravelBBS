//output:../../views/layouts/_message.blade.php

@if (Session::has('message'))
.alert.alert-info
  button.close(type='button', data-dismiss='alert', aria-hidden='true') ×
  {{ Session::get('message') }}
@endif
@if (Session::has('success'))
.alert.alert-success
  button.close(type='button', data-dismiss='alert', aria-hidden='true') ×
  {{ Session::get('success') }}
@endif
@if (Session::has('danger'))
.alert.alert-danger
  button.close(type='button', data-dismiss='alert', aria-hidden='true') ×
  {{ Session::get('danger') }}
@endif
