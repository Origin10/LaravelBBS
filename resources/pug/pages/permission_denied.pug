//output:../../views/pages/permission_denied.blade.php

| @extends('layouts.app')
| @section('title', '無法訪問')
| @section('content')
.col-md-4.col-md-offset-4
  .panel.panel-default
    .panel-body
      | @if (Auth::check())
      .alert.alert-danger.text-center
        | 無法訪問
      | @else
      .alert.alert-danger.text-center
        | 請登入後繼續操作。
      a.btn.btn-lg.btn-primary.btn-block(href="{{ route('login') }}")
        span.glyphicon.glyphicon-log-in(aria-hidden='true')
        | 登入
      | @endif
| @stop
