//output:../../../views/notifications/types/_topic_replied.blade.php

.media
  .avatar.pull-left
    a(href!="{{ route('users.show', $notification->data['user_id']) }}")
      img.media-object.img-thumbnail(alt!="{{ $notification->data['user_name'] }}", src!="{{ $notification->data['user_avatar'] }}", style='width:48px;height:48px;')
  .infos
    .media-heading
      a(href!="{{ route('users.show', $notification->data['user_id']) }}") {{ $notification->data['user_name'] }}
      | 評論了
      a(href!="{{ $notification->data['topic_link'] }}") {{ $notification->data['topic_title'] }}
      | {{-- 回覆刪除按鈕 --}}
      span.meta.pull-right(title!='{{ $notification->created_at }}')
        span.glyphicon.glyphicon-clock(aria-hidden='true')
        | {{ $notification->created_at->diffForHumans() }}
    .reply-content
      | {!! $notification->data['reply_content'] !!}
hr
